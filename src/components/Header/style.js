import { blackColor, redColor } from '../../styles/colors'

export default {
  container: {
    backgroundColor: blackColor,
  },
  logoButton: {
    backgroundColor: redColor,
    borderRadius: 100,
    width: 60,
    height: 60,
    marginLeft: 90,

  },
  logoText: {
    fontSize: 14
  }
}
